# Здесь объекты-значения, например:
#
# * `NewsId`, `RawNewsId`
# * `NewsTitle`, `NewsContent`
# * `SourceName`, `SourceType`
# * `ImportanceScore`
#
# Особенности:
#
# * не имеют собственной идентичности в БД,
# * сравниваются по значению (два `NewsTitle("...")` равны, если строки равны),
# * удобно инкапсулировать инварианты (например, валидировать URL, обрезать слишком длинный заголовок).
