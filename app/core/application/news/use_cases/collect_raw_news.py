# Юзкейс: *«Собрать сырые новости из источников»*.
#
# * Берёт интерфейс `RawNewsRepository` (из домена).
# * Вызывает скрейперы/инфраструктурные сервисы (через абстракции).
# * Преобразует сырые данные в доменные `RawNews`.
# * Сохраняет их в репозиторий.